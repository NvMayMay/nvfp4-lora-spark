# Pinned to the v1.0 measurement-run versions. For the latest published versions, bump and verify against `smoke_tests/`.
# Training stack. vLLM is intentionally not pinned here; it goes in a separate serve venv.
# causal-conv1d is also not pinned here because it needs --no-build-isolation; see README.

torch==2.12.0
transformers==5.8.1
peft==0.19.1
accelerate==1.13.0
safetensors==0.7.0
huggingface-hub==1.14.0
matplotlib==3.10.9
nvidia-modelopt==0.44.0
requests==2.31.0
torchao==0.17.0
psutil==5.9.8
datasets==4.8.5  # scripts/prep_spider.py (public Spider repro; see REPRODUCE_SPIDER.md)
