# local environments
.venv/
venv/
.python-version
__pycache__/
*.pyc
*.pyo

# editor
.vscode/
.idea/
*.swp
.DS_Store

# build / packaging
build/
dist/
*.egg-info/

# model artifacts and adapters: never commit these to the repo
models/
adapters/
*.safetensors
*.bin
*.pt

# training byproducts
*.log
plots/eval_results.json

# machine-local serve configuration (copy from serve/local_env.example.sh)
serve/local_env.sh

# private data + private-data trainers: NEVER public. Spider is pulled at repro
# time by scripts/prep_spider.py, so committed copies under data/ are byproducts.
# The RH/ICH trainers reference private clinical datasets and stay local-only.
data/
scripts/*_ich*.py
scripts/*_rh*.py
